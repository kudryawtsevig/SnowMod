# Gradle properties
org.gradle.jvmargs=-Xmx5G
org.gradle.daemon=false
org.gradle.parallel=true
org.gradle.caching=true

# Increase network timeouts (in milliseconds)
systemProp.org.gradle.internal.http.connectionTimeout=300000
systemProp.org.gradle.internal.http.socketTimeout=300000
systemProp.http.socketTimeout=300000
systemProp.http.connectionTimeout=300000

# Minecraft and Forge versions (matching MDK)
minecraft_version=1.21.1
minecraft_version_range=[1.21.1,1.22)
forge_version=52.1.0
forge_version_range=[52,)
loader_version_range=[52,)
mapping_channel=official
mapping_version=1.21.1

# Mod information
mod_id=snowmod
mod_name=SnowMod
mod_license=MIT
mod_version=1.0.0
mod_group_id=com.snowmod
mod_authors=YourName
mod_description=A mod that adds magical snowflakes and snow blocks to Minecraft
